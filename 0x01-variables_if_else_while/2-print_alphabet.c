#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Hot to odkj'
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 97; a <= 122; a++)
		putchar(a);
	putchar('\n');

	return (0);
}
