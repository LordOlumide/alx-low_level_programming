#include "main.h"

/**
 * _abs - returns the absolute value of an integer
 * @c: value to be returned
 *
 * Return: Absolute vale of input
 */

int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	return (-c);
}
